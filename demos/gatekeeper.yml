client-id: gatekeeper
client-secret: P0Co0c1DoX4KAykhpvyBKSFLtlrclkcY # require for access_type: confidential
# Note the redirection-url is optional, it will default to the URL scheme and host,
# only in case of forward auth it will use X-Forwarded-Proto / X-Forwarded-Host, please see forward-auth section
discovery-url: http://keycloak:8080/realms/master
encryption-key: AgXa7xRcoClDEU0ZDSH4X0XhL5Qy2Z2j
listen: :3000
enable-request-id: true
enable-refresh-tokens: true
enable-login-handler: true
#upstream-url: http://open-echo:7071
upstream-url: http://resource-server:7071
secure-cookie: false
enable-uma: true
enable-logout-redirect: true
enable-metrics: true
enable-logging: true
#enable-uma-method-scope: true
headers:
    myheader1: value_1
    myheader2: value_2
#resources:
    #- uri: /oauth/*
    #  white-listed: true



    #enable-forwarding: true
    #forwarding-username: admin
    #forwarding-password: admin
    # a collection of resource i.e. URLs that you wish to protect, this are simple gatekeeper authorization rules,
    # to get more complex authorization you can look at external authorization section in our documentation
    #resources:
    #    - uri: /oauth/*
    #      white-listed: true
    #    - uri: /admin*
    #    methods:
    #        - GET
    #    roles:
    #        - gatekeeper:admin
    #    require-any-role: true
    #  - uri: /public/*
    #      # Allow access to the resource above
    #    white-listed: true
    #  - uri: /favicon
    #      # Allow access to the resource above
    #    white-listed: true
    #  - uri: /css/*
    #      # Allow access to the resource above
    #    white-listed: true
    #  - uri: /img/*
    #      # Allow access to the resource above
#    white-listed: true
# Adds custom headers